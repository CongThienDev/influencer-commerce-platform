# Backend Tests

This folder contains backend tests for business logic and API behavior.

Current baseline:
- `admin.service.test.mjs`: core Phase 3 admin service smoke-level tests.

Commands:
- `npm test`
- `npm run test:coverage`
- `npm run smoke:admin`

Coverage output is generated under `Backend/coverage/` and is ignored by git.
