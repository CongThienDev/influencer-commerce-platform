# Frontend Tests

Reserved test directory for admin UI tests (unit/integration/e2e).

Suggested future split:
- `unit/` for component and utility tests
- `integration/` for page-level behavior
- `e2e/` for end-to-end admin flow tests

Coverage output should be generated under `Frontend/coverage/` and is ignored by git.
